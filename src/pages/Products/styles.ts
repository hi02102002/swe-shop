import styled from 'styled-components';

export const StyledProducts = styled.div`
   overflow: hidden;
   padding: 3rem 0;

   .container {
      display: flex;
      flex-direction: column;
      row-gap: 3rem;
   }
`;
